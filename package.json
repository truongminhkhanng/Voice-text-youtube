{
  "name": "yt-auto-translate-tts-extension",
  "version": "1.5.0",
  "private": true,
  "description": "Tests and validation for the unpacked Chrome extension.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-extension.js",
    "package:unpacked": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-extension.ps1",
    "preview": "node scripts/serve-static.js",
    "smoke:youtube": "node scripts/smoke-youtube.js"
  }
}
